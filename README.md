# Delta-Neutral ETH Carry Strategy

An on-chain proof-of-concept and real-data backtest of a delta-neutral long-spot / short-perpetual ETH carry strategy.

**Research question:** Does a delta-neutral long-spot / short-perpetual ETH strategy generate attractive historical returns through funding carry, and how can the mechanism be represented on-chain?

> **Important**
> This repository is meant to be safe for a team and safe for a public GitHub submission.
> Do **not** commit `.env`, private keys, RPC secrets, or personal deployment files.

---

## What This Project Does

The strategy holds two legs simultaneously:

| Leg | Position | Direction |
|-----|----------|-----------|
| Spot | ETH/USDT spot exposure (backed by USDC) | Long |
| Perp | ETHUSDT perpetual futures | Short |

Because the legs are equal in notional and opposite in direction, **ETH price moves cancel out** (delta-neutral). The only remaining income is the perpetual funding rate — the periodic payment that long traders pay to shorts when the market is in contango (perp price > spot price).

**Carry = Funding income − Benchmark opportunity cost − Execution costs**

The project is split into two independent layers:

1. **Backtest layer** — runs on real historical data (Coinbase spot + Deribit perp, via CoinAPI) to show what this strategy would have earned historically
2. **Smart-contract layer** — an on-chain proof-of-concept that demonstrates the mechanism correctly in Solidity

---

## Repository Structure

```
├── backtest/               Python backtest engine (real historical data)
│   ├── data_loader.py      Loads the combined hourly spot + perp dataset
│   ├── strategy.py         Daily P&L loop (spot + perp + funding)
│   ├── metrics.py          Sharpe, drawdown, return decomposition
│   ├── run_backtest.py     Entry point — run this
│   ├── audit.py            Rigorous 7-part methodology audit
│   └── main.py             Legacy daily-aggregation backtest
│
├── data/
│   ├── raw/                Place the combined hourly CSV here (see data/README.md)
│   └── processed/          Auto-generated merged data (do not edit)
│
├── contracts/
│   ├── core/
│   │   ├── StrategyVault.sol     Main vault: two-leg position, carry gate, exit logic
│   │   ├── MockPerpEngine.sol    Simulated perpetual futures engine
│   │   └── ArbitrageMath.sol     Pure math library (spot PnL, funding, carry score)
│   ├── tokens/
│   │   └── ArbitrageToken.sol    CARB ERC20 token (course requirement)
│   ├── mocks/                    MockUSDC, MockPriceOracle
│   ├── oracles/                  ChainlinkPriceOracle (Sepolia ETH/USD feed)
│   └── interfaces/               IStrategyVault, IPerpEngine, IPriceOracle
│
├── scripts/                Hardhat deployment and interaction scripts
├── test/                   Unit + integration tests (76 passing)
├── docs/                   Strategy and formula documentation
├── report/                 Final submission report
├── output/
│   ├── charts/             Backtest charts (generated by run_backtest.py)
│   ├── tables/             Backtest CSVs  (generated by run_backtest.py)
│   └── screenshots/        Etherscan / deployment evidence (add manually)
└── frontend/               Simple web UI (MetaMask + ethers.js)
```

---

## Setup Flow

If you are opening this project for the first time, use this order:

1. Run tests to confirm the Solidity system works
2. Run the backtest to regenerate charts and tables
3. Use the frontend locally with MetaMask
4. Deploy to Sepolia
5. Verify contracts on Etherscan

---

## Quick Start

### Prerequisites

```bash
npm install
pip install pandas numpy matplotlib
```

For Sepolia deployment later, copy `.env.example` to `.env` and fill in your own values locally.

> **Do not commit `.env`**
> Every teammate should use their **own** wallet, **own** RPC URL, and **own** Etherscan API key.

### 1 — Run the smart-contract tests

```bash
npx hardhat test
```

Expected output: **76 tests passing**. Demonstrates the full on-chain lifecycle including delta-neutral verification.

### 2 — Run the backtest (requires real data)

Place a combined hourly CSV (Coinbase spot + Deribit perp) in `data/raw/`.
See [data/README.md](data/README.md) for the expected file format. Then:

```bash
python3 backtest/run_backtest.py
```

Outputs:
- `output/tables/backtest_hourly.csv` — hourly P&L breakdown
- `output/tables/backtest_metrics.csv` — summary metrics
- `output/charts/chart1_cumulative_pnl.png` — cumulative net P&L
- `output/charts/chart2_drawdown.png` — drawdown chart
- `output/charts/chart3_pnl_decomposition.png` — spot / perp / funding decomposition

To run the full methodology audit:

```bash
python3 backtest/audit.py
```

### 3 — Run the frontend locally

The frontend uses **MetaMask**.

It does **not** ask users to paste a private key into the page.
Instead:
- MetaMask provides the wallet account
- MetaMask signs transactions
- The frontend only needs contract addresses and ABI files

#### Step A: start a local Hardhat chain

```bash
npx hardhat node
```

#### Step B: deploy the contracts locally

In a second terminal:

```bash
npx hardhat run scripts/deploy-local.ts --network localhost
```

This creates local address files for frontend use.

#### Step C: serve the frontend

In a third terminal:

```bash
cd frontend
python3 -m http.server 8080
```

Then open:

```text
http://127.0.0.1:8080
```

#### Step D: connect MetaMask to the local chain

Add a MetaMask network with:

- RPC URL: `http://127.0.0.1:8545`
- Chain ID: `31337`
- Currency symbol: `ETH`

Then import one of the Hardhat test accounts shown in the `npx hardhat node` terminal.

> **Important**
> These local Hardhat keys are only for local development.
> Never use them on Sepolia or Mainnet.

#### Step E: interact through the frontend

Once MetaMask is connected to the local chain:

- use a funded local test account
- approve USDC
- deposit into the vault
- if connected as owner, open or close the hedge
- read vault state and oracle state from the dashboard

### 4 — Deploy to local node without the frontend

If you only want the contracts locally:

```bash
npx hardhat node                                              # Terminal 1
npx hardhat run scripts/deploy-local.ts --network localhost  # Terminal 2
```

### 5 — Deploy to Sepolia

```bash
npx hardhat run scripts/deploy-sepolia.ts --network sepolia
npx hardhat run scripts/verify-sepolia.ts  --network sepolia
```

After Sepolia deployment:
- contract addresses are saved to `deployed-sepolia.json`
- frontend addresses are saved to `frontend/contracts/addresses.sepolia.json`

---

## Frontend Notes

The frontend is in `frontend/` and uses plain HTML/CSS/JS with `ethers.js`.

### What the frontend needs

- ABI files in `frontend/contracts/`
- a local addresses file:
  `frontend/contracts/addresses.local.json`
- or a Sepolia addresses file:
  `frontend/contracts/addresses.sepolia.json`

An example structure is provided in:

`frontend/contracts/addresses.example.json`

### What should be committed

- frontend source code
- ABI-loading logic
- example config files

### What should not be committed

- `.env`
- personal private keys
- personal RPC keys
- generated local deployment files
- personal local frontend address files

---

## Team Workflow

For a team project, the correct setup is:

1. Everyone pulls the same public repo
2. Each teammate creates their own local `.env`
3. Each teammate can deploy locally and use their own `addresses.local.json`
4. One final Sepolia deployment can be chosen for the submission
5. The final submitted report should reference the final verified Sepolia contracts

> **Recommended**
> Keep the repo generic.
> Keep personal credentials and temporary deployment files local-only.

---

## Strategy Details

### Carry score formula

```
carryScore = (dailyFundingRate × 365) − benchmarkRate − costs
```

Entry is allowed only when `carryScore > leverageScaledThreshold`:

```
threshold = costs + (leverageRatio × riskPremium)
```

Example at 3 bps/day funding, 2% benchmark, 0.5% costs, 5x leverage:
```
carryScore = 1095 − 200 − 50 = 845 bps
threshold  = 50 + 5 × 75     = 425 bps
→ Entry permitted (845 > 425)
```

### Auto-exit conditions

The vault exits automatically if any of these fire:

1. **MARGIN** — perp margin ratio drops below 800 bps (liquidation protection)
2. **CAPITAL** — net loss exceeds 10% of posted collateral
3. **CARRY** — funding no longer covers benchmark + costs
4. **TIME** — position held longer than 30 days

### Delta-neutrality on-chain

`StrategyVault.getVaultState()` computes both legs and reports `netDeltaPnL = spotPnL + shortPerpPnL`. At equal notional, this is approximately zero — verified in `test/integration/FullLifecycle.test.ts`.

---

## Smart Contract Addresses (Sepolia)

| Contract | Address | Etherscan |
|----------|---------|-----------|
| MockUSDC | `0x84EAb608016e21E4618c63B01F7b3b043F4f457e` | [View ↗](https://sepolia.etherscan.io/address/0x84EAb608016e21E4618c63B01F7b3b043F4f457e) |
| ArbitrageToken (CARB) | `0xd2E7bA891e0Ecd142695d04e8Ed79e0C4947922F` | [View ↗](https://sepolia.etherscan.io/address/0xd2E7bA891e0Ecd142695d04e8Ed79e0C4947922F) |
| ChainlinkPriceOracle | `0x27768a80Fb849F6c1bB941C8de62F417Cd968e35` | [View ↗](https://sepolia.etherscan.io/address/0x27768a80Fb849F6c1bB941C8de62F417Cd968e35) |
| MockPerpEngine | `0x478832D03495390E47aFD238A9bA11414096A452` | [View ↗](https://sepolia.etherscan.io/address/0x478832D03495390E47aFD238A9bA11414096A452) |
| StrategyVault | `0x036EA2E331994a04d853B54Ad19D05524eC5b399` | [View ↗](https://sepolia.etherscan.io/address/0x036EA2E331994a04d853B54Ad19D05524eC5b399) |

---

## What Is Mock vs Real

| Component | Real or Mock |
|-----------|-------------|
| Backtest spot price | **Real** — Coinbase ETH/USD, 1-hour bars (CoinAPI) |
| Backtest perp price | **Real** — Deribit ETH/USD perpetual, 1-hour bars (CoinAPI) |
| Funding proxy | **Derived** — lagged `basis_pct / 100 / 8` per hourly bar |
| ETH/USD oracle (Sepolia) | **Real** — Chainlink ETH/USD feed |
| Perpetual exchange | **Mock** — `MockPerpEngine` (no real order book) |
| USDC | **Mock** — `MockUSDC` (mintable for testing) |
| Lending yield | **None** — not part of this strategy |

---

## Limitations

- `MockPerpEngine` does not model slippage, counterparty risk, or liquidation mechanics of a real exchange
- Backtest assumes 1:1 hedge ratio with no rebalancing
- No live market data feed — backtest runs from local CSV files
- The on-chain vault does not connect to the backtest; they are independent layers

---

## Gas Report

Run `npm run test:gas` to generate `gas-report.txt`.

Key operations:
- `openHedge()` — ~333k gas (oracle read + perp open)
- `closeHedge()` — ~110k gas
- `deposit()` — ~170k gas
- `withdraw()` — ~66k gas

---

## Environment Variables

```bash
PRIVATE_KEY=           # Deployer wallet private key
SEPOLIA_RPC_URL=       # Alchemy / Infura Sepolia endpoint
ETHERSCAN_API_KEY=     # For contract verification
```

> **Security**
> `PRIVATE_KEY` should be your wallet's private key **without** the `0x` prefix.
> Store it only in your local `.env`.
> Never paste it into the frontend.
> Never commit it to GitHub.
